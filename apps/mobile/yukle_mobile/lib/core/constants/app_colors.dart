import 'package:flutter/material.dart';

class AppColors {
  // Brand
  static const Color primary = Color(0xFFFF6B00);
  static const Color primaryGold = Color(0xFFFFB627);

  // Backgrounds
  static const Color bgDark = Color(0xFF050608);
  static const Color bgCard = Color(0xFF0D0F12);
  static const Color bgInput = Color(0xFF111318);

  // Text
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFF8B8FA8);
  static const Color textMuted = Color(0xFF4A4D5E);

  // Border
  static const Color border = Color(0xFF1E2030);
  static const Color borderFocus = Color(0xFFFF6B00);

  // Status
  static const Color success = Color(0xFF10B981);
  static const Color error = Color(0xFFEF4444);
}
